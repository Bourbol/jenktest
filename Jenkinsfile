properties ([disableConcurrentBuilds()])

pipeline {
    agent any
    options {
        timestamps()
    }
    stages {
        stage("Just command") {
            steps {
                sh 'sudo mkdir /home/rinmamarun/grofalti'
            }
        }
    }
}
