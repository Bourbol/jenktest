properties ([disableConcurrentBuilds()])

pipeline {
    agent any
    options {
        timestamps()
    }
    stages {
        stage("Just command") {
            steps {
                mkdir /home/rinmamarun/grofalti
            }
        }
    }
}
