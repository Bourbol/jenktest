properties ([disableConcurrentBuilds])

pipeline {
    agent any
    options {
        timestamps()
    }
    stages {
        stage("Just command")
            steps {
                echo "mkdir /home/rinmamarun/grofalti"
            }
    }
}